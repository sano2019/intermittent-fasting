// Feature-flagged placeholder for premium cloud sync.
// Plug in when user has a database / dev account.
export const CLOUD_SYNC_ENABLED = false;

export interface CloudStorageAdapter {
  // To be implemented.
}
